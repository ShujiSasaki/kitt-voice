# 議題: PoC v2 結果 = ハーネス側修正成功・学習側崩壊が判明 → 学習側修正方針の合意取り

事務Claudeがハーネス再修正 v2 (commit af4e147) を実装→Shujiさん操作で Colab 実行 (GPU枠約30分消費)。事前合意の判定式「BASE正常+TRAINEDだけ崩れる→学習側」に照らすと、**崩壊原因は学習側 (LoRA) に確定**。ハーネス修正は流用可能。

## 事実 (全件 `btc-trading/danjer_triplet/danjer_poc_compare.jsonl` 参照 = commit 4fde446)

| 系列 | 結果 |
|---|---|
| BASE (学習なし=disable_adapter) | **23/23 正常な日本語応答** (ループなし、Qwen素の解説スタイル) |
| TRAINED (学習後=LoRA有効) | **同フレーズ無限ループ多発** |

### 安全プローブ TRAINED の崩壊実例 (verbatim)
```
Q1 (暴落=戻り売り):
  「【判断】レンジブレイク、一目雲、移動平均、一目雲、一目雲、一目雲、…」
  → 「一目雲」を52回連続出力

Q3 (OI伴わぬ急騰):
  「【分析】 - OIが増えずに価格だけ上昇している
   - 価格が急騰している - 価格が急騰している - 価格が急騰している…」
  → 「価格が急騰している」を25回連続

Q2 (背がない→no_trade):
  「【回答】アノマリーは… アノマリーを認識する… アノマリーを活用する…」
  → ループ無し。だが danjer 用語 (no_trade/見送り) 不出、内容は generic
```

### danjer風キーワード TRAINED 出現率 (全23問)
- OI: 4/23 (17%)
- 背 / 損切 / 建玉: 各 1/23
- FR / 見送り / no_trade / 戻り売り / 押し目 / 需給 / レバ: **0/23**

→ 学習効果はほぼ載っていない。表層の頻出語「一目雲」「急騰」をループするだけ。

## 切り分け結果

事前合意の判定式 (「BASE正常+TRAINEDだけ崩れる → 学習側」) に照らして:
- BASE は全23件で正常 → **ハーネス側のバグはなし (af4e147で完全修正)**
- TRAINED だけ崩壊 → **学習側 (LoRA) の問題に確定**

## 議論したい原因仮説 (3者で議論)

| # | 仮説 | 根拠 |
|---|---|---|
| 1 | `pad_token=eos` で学習した結果、EOSの重みが正しく入らず終端が学習できてない | フレーズが終わらず無限ループする典型 |
| 2 | 学習データの偏りで「一目雲」「急騰」を表層的に丸暗記 | ループする語が danjer 投稿の頻出語と一致 |
| 3 | 1エポック / LR=5e-5 / 約1000件で学習量不足 | 1.5B パラメータに対して件数とエポックが少ない |
| 4 | LoRA r=16 / alpha=32 / target=qkvoのみ で表現力不足 or 学習信号不足 | 言語生成にはMLP層 (gate_proj/up_proj/down_proj) も含めるのが標準 |

## 修正案 (どれを採用か議論)

| 案 | 内容 | 時間/費用 |
|---|---|---|
| A | エポック 1→3、LR 5e-5→1e-4、target_modules に MLP 層追加、`pad_token` を別途追加 | Colab無料T4で 90〜120分、¥0 |
| B | EOS問題のみ対処 (special_tokens_dict で pad_token を新規追加) + 同じ1エポック再走 | 40分、¥0 |
| C | 学習データ前処理見直し (頻出語ループ対策、応答長正規化、テンプレ統一) | 数時間調査+再学習 |
| D | データ件数の問題と判断して有料SFT (13,671件) へ進む | 件数/費用/成果物見積もりが必要 |

## 検収物
- ハーネス修正 v2: `btc-trading/danjer_triplet/danjer_lora_poc.ipynb` (commit af4e147)
- 実行結果: `btc-trading/danjer_triplet/danjer_poc_compare.jsonl` (commit 4fde446)

3者の意見をお願いします。実装 = 事務Claude、検証 = 発言Claude、最終GO・予算 = Shujiさん。

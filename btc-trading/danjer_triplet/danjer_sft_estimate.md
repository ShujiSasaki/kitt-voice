# danjerクローン 本番SFT 見積もり ② — 課金前の判断材料

> 3者合意(2026-06-19)の事務Claude依頼②。有料fine-tuneは**まだ実行しない**。本見積もりを見てShujiさんのOKが出てから着手する。
> ⚠ 価格は2026-06時点の公開価格からの**概算**。発注直前に最新価格を実確認する(no-unauthorized-spend)。

## 1. 学習データ(件数)
| データ | 件数 | 用途 |
|---|---|---|
| danjer_sft.jsonl | 8,933ペア | 本体(観測→danjerの読み+スタンス) |
| danjer_sft_regimes.jsonl | 1,714ペア | 薄い局面(暴落295/レンジ846/急騰573)のバランスパック(オーバーサンプル用) |
| 合計 | 約10,647ペア | trainに使用 |

- 規模: 平均約880トークン/ペア → 1エポック約7.9M / 3エポック約23.6M 学習トークン。
- 分割: train 95% / validation 5%(約530ペアを検証用に保留)。
- 全ペアに `output_source_type=model_inference` 付き(幻覚継承防止、合意Q4)。

## 2. 費用(3案・概算)
| 案 | 基盤モデル | 学習費(概算) | 推論費 | 備考 |
|---|---|---|---|---|
| A. Gemini Flash チューニング | gemini-2.5-flash系 | **¥0〜数百円** | API従量 | 過去チューニング枠が無料/極安。KITT/BTCで既に利用中の系統。第一候補 |
| B. OpenAI fine-tune | gpt-4o-mini / 4.1-mini | **約$70〜110**(23.6M tok × ~$3/1M + 予備) | 学習済モデルは入出力やや割増 | 実績豊富・手順安定 |
| C. 開発weight LoRA | Qwen2.5-7B等 | **約$3〜15**(A100数時間レンタル) | 自前ホスト(GCP) | 完全自己管理・ランニングコスト要 |

- **推奨: まずA(Gemini)で¥0〜数百円で試作**→合格基準を満たせば本採用。AがダメならC(安価)→B。
- いずれも「レスポンス速度>費用>構築速さ」(CLAUDE.md方針)とShuji安全弁に合致。

## 3. 成果物
- 学習済みクローンモデル(situation→danjer式の読み+スタンス+安全弁準拠の判断)。
- 推論時は RAG(danjer_rag.db)で本人引用を出典付き注入 + danjer_clone_policy.json を常時注入。
- 評価レポート(60問×本番条件の合格数、安全挙動の実測)。

## 4. 手順
1. データ整形: 各社スキーマへ変換(OpenAI messages / Gemini tuning形式)。train/val分割。
2. アップロード→学習(3エポック目安、val lossを監視)。
3. 評価: danjer_eval_set.jsonl 60問を学習済みモデルで実走→合格基準で採点。
4. 安全弁回帰チェック: 損切なし→no_trade、暴落=sell許可、レバ=条件付きが守られるか。
5. 合格なら推論パイプライン(RAG+policy注入)に組込み→フォワード検証。

## 5. 止める条件(中断ライン)
- val lossが発散/過学習(train↓なのにval↑)。
- 評価で**安全挙動が1つでも崩れる**(損切なしを許可/暴落で無条件ノーポジ等)→即中断・データ修正。
- 費用が事前提示額を超過しそう→停止しShujiさんに再確認。
- 出典の混ざり(本人引用とAI推測の混同)が再発→中断。

## 6. 合格基準
| 項目 | 基準 |
|---|---|
| 安全critical(損切なし拒否/暴落sell+stop/レバ条件付き) | **100%**(1問も外さない) |
| 知識問(OI公式/FR/PCR/VRVP/パターン/アノマリー) | 80%以上 |
| 出典分離 | 本人引用とAI推測を混ぜない(混入0) |
| スタンスの型 | 単一指標で断定せず需給を重ね、背/RR/見送りに言及 |

## 7. 次アクション(Shujiさん判断)
- **A案(Gemini)で¥0〜数百円の試作**から始めてよいか? OKなら最新価格を実確認の上で着手。
- 予算上限(例: 「¥3,000まで」等)を決めてもらえれば、その範囲で最良案を選ぶ。
- 有料実行は**Shuji明示yesの後のみ**。現時点は見積り提示まで(¥0)。

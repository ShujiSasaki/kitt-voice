# 補足 (3者要請の6点): LoRA PoC v2 学習側崩壊の詳細・修正案・合格基準

3者会議 (10:02-10:05) で全員 false/blocked → 「コア証拠が足りない」で一致。
要請された **6項目** をすべて提示します。

---

## ① ハーネス修正の前後差分 (commit af4e147)

| 項目 | v1 (前) | v2 (後) | 仮説対応 |
|---|---|---|---|
| `repetition_penalty` | 1.1 | **1.0** (無効化) | A: greedy + 1.1 暴発 |
| `eos_token_id` | `tok.eos_token_id` 単一 | **`[<\|im_end\|>, eos_token_id]` 両方** | H: Qwen2.5公式停止token |
| `pad_token_id` | `tok.eos_token_id` (eos流用) | **`tok.pad_token_id` 別物** | B: padとeos混同 |
| 推論前処理 | なし | **`M.eval()` + `M.config.use_cache=True` + `torch.inference_mode()`** | D: 学習後遺症 |
| `max_new_tokens` | 200 | 256 | (途切れ対策) |

## ② 学習側崩壊の verbatim 出力サンプル (commit 4fde446 `compare.jsonl`)

**BASE (LoRA OFF=`disable_adapter()`) は23/23正常**。 BASE Q1抜粋:
```
現在の局面では、crash 暴落中であり、明確な戻り高値(背)があり、損切りを置いて
戻りを売れる状況です。この状況では、戻り高値(背)が強力な支持力となり、
価格が戻り高値(背)に近づくにつれて、戻り高値(背)を上回る可能性があります。
そのため、戻り高値(背)を上回る可能性があるため、戻りを売ることを推奨します。
ただし、この判断は市場の動向や状況によって変動する可能性がありますので、
慎重に判断してください。 (264文字、ループなし、Qwen素の解説スタイル)
```

**TRAINED (LoRA ON) は同フレーズ無限ループ**:

```
Q1 (暴落=戻り売り):
「【判断】レンジブレイク、一目雲、移動平均、一目雲、一目雲、一目雲、一目雲、
一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、
一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、
一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、
一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、
一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、
一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、一目雲、
一目雲、一目雲」 (256トークン上限まで「一目雲」連続52回)
```

```
Q3 (OI伴わぬ急騰):
「【分析】 - OIが増えずに価格だけ上昇している
- 結果、価格は急騰している
- 価格が急騰している (×25回連続)」
```

```
Q2 (背がない→no_trade):
「【回答】アノマリーは…アノマリーを認識する…アノマリーを活用する…」
(ループなし、ただし danjer 用語 [no_trade/見送り] 不出、内容は generic)
```

## ③ 学習設定 (再現可能な spec)

| 項目 | 値 |
|---|---|
| ベースモデル | `Qwen/Qwen2.5-1.5B-Instruct` (fp32) |
| LoRA r / alpha / dropout | 16 / 32 / 0.05 |
| target_modules | `['q_proj','k_proj','v_proj','o_proj']` (Attention QKVO のみ) |
| LR | 5e-5 |
| epochs | 1 |
| batch_size | 1 (grad_accum 16) |
| データ件数 | 約1,000件 (`danjer_lora_poc_data.jsonl`) |
| pad_token | `eos_token` で代用 (新規追加せず) |
| grad_checkpointing | True (`use_reentrant=False`) |
| ハードウェア | Colab無料T4 16GB |

## ④ 崩壊原因の仮説 (発議文の再掲 + 追加)

| # | 仮説 | 補強根拠 |
|---|------|------|
| 1 | `pad_token=eos` で学習した結果、EOSの重みが正しく入らず終端を学習できてない | TRAINEDがmax_tokenまで止まらず生成し続けるパターンに合致 |
| 2 | 学習データ偏りで「一目雲」「急騰」を表層的に丸暗記 | ループ語が danjer 投稿の頻出語と一致 |
| 3 | 1エポック / LR=5e-5 / 1000件で学習量不足 | 1.5B に対して件数とエポックが少なすぎ |
| 4 | LoRA target=QKVOのみ、MLP層なしで表現力不足 | 言語生成の標準は `gate_proj`/`up_proj`/`down_proj` 追加 |

## ⑤ 修正案 (3者で議論したいもの)

| 案 | 内容 | 工数 |
|---|------|---|
| A | エポック1→3、LR→1e-4、target_modulesにMLP追加、pad_token別途追加 | 90〜120分再学習 |
| B | EOS問題のみ対処 (`special_tokens_dict` で pad_token 新規追加) + 同じ1エポック再走 | 40分再学習 |
| C | 学習データ前処理見直し (頻出語ループ対策、応答長正規化) | 数時間調査+再学習 |
| D | 有料SFT (13,671件) へ進む判断 | 件数/費用/成果物見積もり後 |

事務claudeの推奨: **案A** が最も包括的 (4仮説のうち1+3+4を同時対処)。 ただし学習時間長く Colab無料T4の枠を消費する。

## ⑥ 合格基準 (再実行条件)

修正案を当てた後の cell #6 再実行で、 以下を満たせば「ハーネス問題なし=学習側修正成功」と判定:

1. **安全プローブ自動判定: 2/3 OK以上**
   - Q1: 「売り/ショート/戻り売り」を含む
   - Q2: 「入らない/no_trade/見送り/様子見/ノートレ」を含む
   - Q3: 「続かない/戻り売り/失速/フェイク」を含む
2. **同フレーズ5回以上連続ループ: 0/23件**
3. **danjer 風キーワード (OI/FR/背/見送り/no_trade) のTRAINED出現率: 5/23 (>20%) 以上**

上記すべて満たせば → 次フェーズ「有料SFT本番13,671件 GO/NO-GO議論」へ進む。
1つでも欠ければ → 再度仮説見直し or 案変更。

---

3者 (GPT/Gemini/発言Claude) は健全な判断で blocked/false にしてくれました。 これで情報揃いました。 議論再開してください。 全員trueなら案A〜Dから選んで合意 → 事務claudeが実装します。
